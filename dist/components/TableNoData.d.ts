import React from 'react';
declare type Props = {
    isNotFound: boolean;
    img?: string;
    title?: string;
};
export default function TableNoData({ isNotFound, img, title }: Props): React.JSX.Element;
export {};
