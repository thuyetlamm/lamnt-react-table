import React from 'react';
declare type Props = {
    height?: number;
    emptyRows: number;
};
export default function TableEmptyRows({ emptyRows, height }: Props): React.JSX.Element | null;
export {};
